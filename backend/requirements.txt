# VoiceShield AI — Python Dependencies
# All packages are 100% free / open-source. No paid services.

# ── Web Framework ────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

# ── Numerics & Audio ─────────────────
numpy>=1.26.0
librosa>=0.10.0
scipy>=1.12.0
soundfile>=0.12.0

# ── ML / ONNX ───────────────────────
onnxruntime>=1.18.0       # Apache-2.0 — free
torch>=2.2.0              # BSD — free
onnx>=1.16.0              # Apache-2.0 — free
transformers>=4.40.0       # Apache-2.0 — free (Cloud XLS-R + wav2vec2 fine-tunes)

# ── PDF Reports ──────────────────────
reportlab>=4.1             # BSD — free

# ── HTTP Client (alerts) ────────────
httpx>=0.27.0              # BSD — free
cryptography>=41.0.0          # BSD/Apache-2.0 - free (AES-256-GCM for I4C external anchor)

# ── Async Email ──────────────────────
aiosmtplib>=3.0.0          # MIT — free

# ── Configuration ────────────────────
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# ── Testing ──────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
